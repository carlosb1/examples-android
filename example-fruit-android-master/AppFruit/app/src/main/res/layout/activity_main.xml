<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.hackvg.foods.views.activities.DrawerActivity"
    >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".MainActivity"
        >

        <com.example.foods.views.custom_views.AutofitRecyclerView
            android:id="@+id/activity_foods_recycler"
            android:layout_width="match_parent"
            android:paddingTop="?android:attr/actionBarSize"
            android:columnWidth="@dimen/item_food_width"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            />

        <android.support.v7.widget.Toolbar
            android:id="@+id/activity_foods_toolbar"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:minHeight="?attr/actionBarSize"
            android:background="@color/theme_primary"
            >

            <com.example.foods.views.custom_views.LobsterTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textSize="22sp"
                android:textColor="#FFF"
                />
        </android.support.v7.widget.Toolbar>

        <ProgressBar
            android:id="@+id/activity_foods_progress"
            android:layout_width="55dp"
            android:layout_height="55dp"
            android:layout_marginTop="@dimen/activity_vertical_margin"
            android:layout_gravity="center"
            android:visibility="gone"
            style="?android:attr/progressBarStyleLarge"
            />

    </FrameLayout>


</android.support.v4.widget.DrawerLayout>